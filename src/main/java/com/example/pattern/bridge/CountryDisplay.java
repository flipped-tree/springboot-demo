package com.example.pattern.bridge;

public class CountryDisplay extends Display {
    public CountryDisplay(DisplayImpl impl) {
        super(impl);
    }

    public void multiDisplay(int times) {
        open();
        for (int i = 0; i < times; i++) {
            print();
        }
        close();
    }
}
